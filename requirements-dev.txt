# ========== 开发/测试依赖（与生产依赖分离，见 requirements.txt） ==========
# 安装: pip install -r requirements-dev.txt（会先安装 -r requirements.txt 中的生产依赖）

-r requirements.txt

pytest>=8.3.0
pytest-asyncio>=0.25.0
pytest-cov>=6.0.0
pytest-mock>=3.14.0
